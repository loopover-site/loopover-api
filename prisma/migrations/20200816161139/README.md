# Migration `20200816161139`

This migration has been generated by xWafl at 8/16/2020, 12:11:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."knex_migrations" (
"id" SERIAL,
"name" text   ,
"batch" integer   ,
"migration_time" timestamp(3)   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."knex_migrations_lock" (
"index" SERIAL,
"is_locked" integer   ,
PRIMARY KEY ("index"))

CREATE TABLE "public"."leaderboards" (
"id" SERIAL,
"category" text  NOT NULL ,
"subCategory" text  NOT NULL ,
"date" integer  NOT NULL ,
"evidence" text  NOT NULL ,
"userHasAccount" boolean  NOT NULL ,
"username" text  NOT NULL ,
"time" integer  NOT NULL DEFAULT 0,
PRIMARY KEY ("id"))

CREATE TABLE "public"."sessions" (
"id" SERIAL,
"key" text  NOT NULL ,
"maxAge" integer  NOT NULL ,
"session" jsonb  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."users" (
"id" SERIAL,
"username" text  NOT NULL ,
"password" text  NOT NULL ,
"role" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "users.username_unique" ON "public"."users"("username")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200816161139
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,45 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model knex_migrations {
+  id             Int       @default(autoincrement()) @id
+  name           String?
+  batch          Int?
+  migration_time DateTime?
+}
+
+model knex_migrations_lock {
+  index     Int  @default(autoincrement()) @id
+  is_locked Int?
+}
+
+model leaderboards {
+  id             Int     @default(autoincrement()) @id
+  category       String
+  subCategory    String
+  date           Int
+  evidence       String
+  userHasAccount Boolean
+  username       String
+  time           Int     @default(0)
+}
+
+model sessions {
+  id      Int    @default(autoincrement()) @id
+  key     String
+  maxAge  Int
+  session Json
+}
+
+model users {
+  id       Int    @default(autoincrement()) @id
+  username String @unique
+  password String
+  role     String
+}
```


